slam_toolbox:
  ros__parameters:

    map_frame: map
    odom_frame: odom
    base_frame: base_link

    scan_topic: /scan_filtered

    throttle_scans: 1
    scan_queue_size: 50
    transform_timeout: 0.5

    use_sim_time: true
